# Минимальные версии: точные старые пины не собираются на Python 3.14
# (нет готовых wheel под 3.14, а сборка pydantic-core/aiohttp из исходников
#  падает на PyO3 <= 0.22). Поэтому используем нижние границы с 3.14-wheel.
aiogram>=3.29.0
pydantic-settings>=2.14.0
SQLAlchemy>=2.0.36
aiosqlite>=0.20.0
asyncpg>=0.30.0
APScheduler>=3.10.4
google-genai>=0.3.0
tzdata>=2024.1
pytest>=8.3.3
pytest-asyncio>=0.24.0
